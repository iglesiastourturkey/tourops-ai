Implement only temporary-password onboarding and fix the remaining Google sign-in loading issue.

Goals:

1. Keep both existing login pages:
- Yönetici Girişi
- Personel Girişi

2. Login rules:
- super_admin/admin: username + password
- operations/field_operations/accounting/guide: email + password
- Google remains optional for all allowed roles

3. Temporary password onboarding:
- super_admin can create or invite a user
- system generates a unique strong temporary password for that user
- show the temporary password only once to the super_admin
- never store plaintext passwords in PostgreSQL
- send the temporary password securely to the user only if the existing email workflow supports it; otherwise show it once for manual delivery
- mark the user as requiring password change
- on first successful login, redirect to a mandatory “Şifrenizi Değiştirin” page
- block access to the application until the password is changed
- after successful change, clear the password-change-required state and continue to the correct role dashboard

4. Existing super admin:
- use the existing aydin254@gmail.com profile
- keep role=super_admin and is_active=true
- do not create a duplicate
- allow assigning a username
- allow generating one temporary password for initial access

5. User Management actions:
- Geçici Şifre Oluştur
- Şifre Sıfırlama Bağlantısı Gönder
- Kullanıcı Adı Ata / Değiştir
- Hesabı Aktifleştir / Pasifleştir

6. Google sign-in loading bug:
- verify Google OAuth callback creates and activates a browser session
- verify setActive() is called with the created session
- verify GET /api/profiles/me and GET /api/profiles/me/permissions return 200
- never leave the user indefinitely on “Yetki doğrulanıyor...”
- on failure show Turkish error UI with:
  - Yeniden Dene
  - Çıkış Yap
  - Ana Sayfaya Dön

7. Security:
- use Clerk for password hashing and verification
- never log passwords
- never persist plaintext passwords
- do not use one shared password for multiple users
- audit temporary password creation and forced password change
- preserve RBAC and permission matrix

8. Scope:
Do not modify business modules, accounting, AI, operations, database schema unless a minimal field is strictly required to track forced password change.

9. Verification:
Test:
- existing super_admin temporary-password login
- forced password change on first login
- admin username/password login
- staff email/password login
- Google login
- disabled user denial
- duplicate username rejection
- no plaintext password in database

Run:
- frontend TypeScript
- API TypeScript
- production build

Report only:
- modified files
- temporary-password flow
- Google sign-in root cause/fix
- security controls
- build results
- manual Clerk settings still required