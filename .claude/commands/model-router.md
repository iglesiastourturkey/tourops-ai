---
description: Görev karmaşıklığını sınıflandırır ve en uygun/en ucuz modeli önerir (master plan Bölüm 14 tablosuna göre).
---

Aşağıdaki görevi TourPilot master plan'ındaki model seçim tablosuna göre sınıflandır:

Basit → Haiku (metin, küçük CSS, mekanik düzeltme)
Standart → Sonnet (normal frontend/backend implementasyonu)
Karmaşık → Opus (mimari/root cause) → sonra Sonnet (implementasyon)
Kritik → Opus (auth/security/data loss/resmi dış yazma)

Görev: $ARGUMENTS

Şunu raporla:
1. Hangi kategoriye giriyor ve neden
2. Önerilen model
3. Hangi .claude/agents/*.md dosyasının bu görev için uygun olduğu
